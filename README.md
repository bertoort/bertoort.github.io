# bertoort.github.io

Personal site. Dark page with a flashlight cursor — move the lamp around to reveal content and find the ghost.

## Local development

Open `index.html` in a browser. No build step required.

## Deploy

Push to `main` and enable GitHub Pages from **Settings → Pages → Source: Deploy from a branch → `main` / `/ (root)`**.
